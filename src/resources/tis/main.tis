include("bar.tis");

function self.ready() {
    self.$(#verify).on("click", function() {
        view.dialog({
            url: "result.htm",
            height: "600px",
            parameters: {
                result: "hello",
            },
        });
    });
}
