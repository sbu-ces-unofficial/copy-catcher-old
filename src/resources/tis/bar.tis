namespace bar {

    function incrementProgess(val) {
        let progress = self.$(#verify-bar).value;
        let currentProgress = progress + val;
        self.$(#verify-bar).value = currentProgress;
        self.$(#verify-bar-text).value = currentProgress.toString() + "%";
    }

}
